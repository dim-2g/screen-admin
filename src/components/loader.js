import React from 'react';

export default () => {
    return (
        <div className="spinner-border loader-fixed" role="status">
          <span className="sr-only">Загрузка...</span>
        </div>
    );
}